# macOS
.DS_Store

# Astro / Node (arrivera à l'étape suivante, mais autant l'ignorer dès maintenant)
node_modules/
dist/
.astro/

# Claude Code (config locale à la machine)
.claude/settings.local.json
